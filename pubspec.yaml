name: glass_shimmer
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: 3.5.0-203.0.dev
  flutter: 3.23.0-13.0.pre.22

dependencies:
  flutter:
    sdk: flutter
  flutter_shaders: ^0.1.2
  leancode_hooks: ^0.0.6

dev_dependencies:
  leancode_lint: ^12.1.0

flutter:
  uses-material-design: true

  shaders:
    - shaders/border.frag
    - shaders/pillow_rect.frag
